diff --git a/input/drivers/android_input.c b/input/drivers/android_input.c
index df37d1a051..1ce48c9bc3 100644
--- a/input/drivers/android_input.c
+++ b/input/drivers/android_input.c
@@ -1019,6 +1019,10 @@ static INLINE void android_input_poll_event_type_keyboard(
       *handled = 0;
 }
 
+#ifdef HAVE_RICOTTA_IGM
+extern int ricotta_bridge_intercept_key(int keycode, int action);
+#endif
+
 static INLINE void android_input_poll_event_type_key(
       struct android_app *android_app,
       AInputEvent *event, int port, int keycode, int source,
@@ -1026,6 +1030,14 @@ static INLINE void android_input_poll_event_type_key(
 {
    uint8_t *buf = android_key_state[port];
    int action   = AKeyEvent_getAction(event);
+
+#ifdef HAVE_RICOTTA_IGM
+   if (ricotta_bridge_intercept_key(keycode, action))
+   {
+      *handled = 1;
+      return;
+   }
+#endif
    int keysym   = keycode;
 
    /* Handle 'duplicate' inputs that correspond
@@ -1832,6 +1844,13 @@ static void android_input_poll(void *data)
    android_input_t *android        = (android_input_t*)data;
    settings_t            *settings = config_get_ptr();
 
+#ifdef HAVE_RICOTTA_IGM
+   {
+      extern void ricotta_bridge_poll_commands(void);
+      ricotta_bridge_poll_commands();
+   }
+#endif
+
    while ((ident =
             ALooper_pollAll(settings->uints.input_block_timeout,
                NULL, NULL, NULL)) >= 0)
